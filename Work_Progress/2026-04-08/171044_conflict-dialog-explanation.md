# 任务记录

## 任务名称
- 解释文档冲突弹窗含义

## 执行时间
- 开始时间：2026-04-08 17:10:44
- 结束时间：2026-04-08 17:15:48

## 仓库根目录
- C:\Users\Administrator\Desktop\BrainFlow

## 任务目标
- 说明截图中的“文档冲突”弹窗为什么出现，以及两个处理按钮分别代表什么。

## 解决的问题
- 已定位截图对应前端弹窗为存储同步冲突保护对话框。
- 已确认触发条件为本地待上传操作的 `baseVersion` 与云端当前版本不一致，服务端返回版本冲突。
- 已确认截图中的“云端版本未记录 / 哈希未知”表示云端当前查不到这份文档记录，`cloudRecord` 为 `null`。
- 已确认当前实现仍展示“采用云端版本”按钮，但后端在 `cloudRecord` 为空时会直接报错，属于边界场景未处理完整。

## 问题原因
- 本地缓存里有一份待同步文档，但服务端校验时发现该文档的当前云端版本与本地提交所基于的版本不一致，因此阻止自动覆盖并弹出冲突对话框。截图所示场景下，云端侧甚至没有当前记录，通常意味着文档曾被云端删除、工作区被恢复/重建，或本地缓存基于过期版本继续提交。

## 尝试的解决办法
1. 确认仓库根目录与任务开始时间，准备本轮任务记录。
2. 搜索“文档冲突”“本地缓存版本”“云端权威版本”等文案，定位对应源码文件。
3. 检查冲突弹窗组件与同步模块，确认触发条件和按钮行为。
4. 核对服务端版本冲突判定与冲突解决逻辑，确认 `cloudRecord` 为空时的实际行为。

## 是否成功解决
- 状态：成功
- 说明：已确认弹窗含义、触发机制、两个按钮的大致语义，以及截图场景下“采用云端版本”按钮存在后端无法处理的边界问题。

## 相关文件
- src/features/storage/ui/StorageConflictDialog.tsx
- src/features/storage/sync/cloud-sync-orchestrator.ts
- shared/sync-contract.ts
- server/services/sync-service.ts
- server/repos/postgres-sync-repository.ts

## 遗留问题/下一步
- 如果要修复体验，建议在 `cloudRecord` 为空时禁用或隐藏“采用云端版本”按钮，并改成“保留本地为新副本”或“忽略本地更改”等更准确文案。
